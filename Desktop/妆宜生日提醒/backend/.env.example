# 后端服务端口
PORT=4000

# PushPlus 推送 token（去 https://www.pushplus.plus/ 注册后获取）
PUSHPLUS_TOKEN=

# 每日定时检查时间（cron 格式，默认每天晚上 8:00）
DAILY_CRON=0 20 * * *

# 时区
TZ=Asia/Shanghai

# 当明天没有人生日时，是否仍然推送提醒（true / false）
PUSH_WHEN_EMPTY=false
